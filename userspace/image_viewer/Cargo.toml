[package]
name = "image_viewer"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "image_viewer"
path = "main.rs"

[dependencies]
librost = { path = "../../librost" }

[profile.release]
panic = "abort"
lto = true
opt-level = "z"  # Optimize for size

[workspace]
resolver = "2"
members = [
    "kernel",
    "librost",
    "userspace/shell",
]

# Shared profile settings for all workspace members
[profile.release]
opt-level = 2
lto = true
panic = "abort"

[profile.dev]
panic = "abort"
